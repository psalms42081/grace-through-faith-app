You are working inside our existing Bible study app codebase. We already have:
- KidsModeContext with setPin, verifyPin, and PIN-gated exit
- age tiers: "Little Lambs", "Young Disciples", "Young Disciples Plus"
- database table: childProfiles
- Family tab already displays child cards with per-child stats (stories completed, quiz averages, badges)

Goal:
Connect these pieces into a cohesive parent-controlled Kids Mode flow, with minimal UI and clean UX.

PRIMARY REQUIREMENTS (build these end-to-end)

1) Parent PIN Setup (surfaced in UI)
- Add a simple Parent Controls entry point in the main app (Family tab or You tab):
  - "Parent Controls"
  - inside: "Kids Mode PIN"
- If no PIN exists:
  - show "Set PIN" flow (4-digit numeric)
  - confirm PIN (enter twice)
- If PIN exists:
  - show "Change PIN" (requires current PIN)
  - show "Disable PIN" (requires current PIN) [optional if time]
- Store PIN securely if possible (platform secure storage). If the project already uses secure storage, use it; otherwise use existing persistent storage but isolate it in KidsModeContext storage layer.

2) “Who’s reading today?” Child Picker when entering Kids Mode
- When parent taps Kids Mode (the two-people icon), show a modal/screen BEFORE entering Kids UI:
  - title: "Who’s reading today?"
  - list childProfiles as selectable cards (avatar/initials, name, age tier)
  - option: "Add child" (name + age tier only; keep it light)
- Selecting a child starts Kids Mode session for that child:
  - set activeChildProfileId in KidsModeContext (or dedicated KidsSessionContext)
  - persist lastActiveChildProfileId for quick next time

3) Tier selection behavior
- Each child profile has a tier. Use it to filter content automatically.
- Remove/avoid asking tier every time if it’s already stored per child.
- If you currently ask tier on entry, replace it with per-child tier or keep it as “Edit tier” inside Parent Controls.

4) Per-child tracking inside Kids Mode
- Ensure all Kids Mode progress writes are scoped to activeChildProfileId:
  - stories read/completed
  - quiz attempts/averages
  - badges/stars
  - memory verses marked memorized
- If schema already supports these links, wire them through.
- If schema currently stores generic kids progress, migrate logically by adding childProfileId field and defaulting to lastActive or first child.

5) PIN-gated exit + switching
- Keep current PIN-gated exit behavior.
- Add an in-Kids header button: "Switch Child" (or "Change Reader")
  - should require PIN (same as exit) OR at least allow without PIN if parent wants; default to PIN-gated for safety.
  - opens the same child picker modal and sets activeChildProfileId.

UX / DESIGN REQUIREMENTS
- Kids Mode must be obviously a different mode:
  - show a "KIDS MODE" label in the header (small caps)
  - change Exit button label to "Exit Kids Mode"
- Keep the child experience minimal:
  - avoid forcing logins
  - avoid adult menus inside kids mode
- Parent flows should live outside kids mode (main app side) except the PIN-gated Switch/Exit.

IMPLEMENTATION NOTES
- Prefer adding:
  - KidsSessionContext { activeChildProfileId, lastActiveChildProfileId }
  - or extend KidsModeContext if simpler
- Update routing so the entry flow is:
  main app -> child picker -> kids tabs
- Use existing UI components + styling conventions:
  - same typography, pressed states, dividers, etc.
- Add unit tests or simple sanity checks where appropriate:
  - entering kids mode without any child profiles should prompt “Add child”
  - progress updates should affect only active child
  - Family tab should reflect updated per-child stats after kids usage

DELIVERABLES
- Working UI for:
  - Parent Controls > Set/Change PIN
  - Kids Mode entry child picker
  - Kids Mode active child session
  - PIN-gated Exit + PIN-gated Switch Child
- Minimal but clear copy text throughout (no walls of text)
- Ensure no regressions to existing Family tab child cards and analytics

After implementing, provide:
- list of files changed
- short explanation of state flow (where activeChildProfileId is stored, how progress queries are scoped)
- any TODOs if you had to leave small items incomplete