We need to diagnose and fix a deployment/runtime mismatch in this Expo + Replit app.

Context:
When I open the app through Expo Go, I see one version of the app.
When I open the deployed Replit link, I see a different version.
This suggests development and deployed environments are out of sync, or the deployment is not serving the same app/runtime as Expo Go.

Goal:
Get this project back on track by identifying exactly why Expo Go and the Replit deployed link are showing different versions, then fix it so deployment reliably reflects the intended current build.

What I need from you:
Do a full deployment/runtime audit of this project and tell me, with precision:
1. What code/runtime Expo Go is loading
2. What code/runtime the Replit deployed link is loading
3. Whether the deployed link is serving:
   - a real production build
   - an Expo preview launcher
   - a web build
   - an old cached build
   - or a different entry point than Expo Go
4. Whether the backend, database, migrations, seeds, and environment variables differ between development and deployment
5. Exactly what needs to be changed so the deployed app matches the intended current version

Important instructions:
- Do not just guess
- Inspect the actual project files and configuration
- Trace the startup/build/deployment path carefully
- Explain findings in plain English
- Make fixes directly where appropriate
- Prefer stability and correctness over adding features
- Do not introduce new features unless required for the fix
- Preserve existing app functionality

Please inspect these areas carefully:

A. Project/runtime structure
- package.json
- app.json / app.config.js / app.config.ts
- expo configuration
- entry points
- scripts for dev, build, start, deploy
- whether this is meant to be:
  - Expo Go mobile preview
  - Expo web
  - native build
  - hybrid frontend + backend on Replit

B. Replit deployment config
- .replit
- replit.nix
- deployment/start commands
- run commands
- web server config
- port binding
- whether Replit is serving the frontend, backend, or only a launcher page

C. Environment differences
Check whether dev and deployment use different:
- API base URLs
- EXPO_PUBLIC_* variables
- NODE_ENV behavior
- database URLs
- feature flags
- secrets
- asset paths
- routing settings

D. Frontend differences
Inspect:
- root app entry
- Expo Router config
- tabs/home screen
- any environment-based conditional rendering
- any code that switches behavior between web/dev/prod/mobile

E. Backend/data differences
Inspect:
- server startup
- database connection
- migrations
- seed scripts
- production seed behavior
- whether deployment has stale or missing data
- whether dev points to a different DB than deployed app

F. Build/deploy reliability
Check for:
- interactive prompts blocking deployment
- migrations requiring confirmation
- seeds failing silently
- build scripts not running in deployment
- stale artifacts
- cached output
- wrong working directory
- unsupported Expo/Replit deployment pattern

Specific tasks:
1. Determine the exact reason the Replit deployed link shows a different version from Expo Go
2. Identify whether Expo Go is loading a development bundle while the Replit URL is loading:
   - an older deployment
   - a web-only version
   - a preview shell
   - or another app target entirely
3. Find the exact config/code causing the mismatch
4. Fix the mismatch
5. Make deployment deterministic and non-interactive
6. Ensure the intended current app version is what gets deployed
7. Confirm whether the deployed Replit URL should even match Expo Go 1:1, and if not, explain the architectural reason

Deliverables I want:
1. A short diagnosis summary:
   - what was wrong
   - why Expo Go and Replit differed
   - what you changed

2. A technical breakdown:
   - files inspected
   - root cause
   - config/runtime differences
   - database/env/deploy differences if any

3. Concrete fixes:
   - edited files
   - exact changes made
   - any commands I need to run

4. Final verification checklist:
   - how to confirm dev and deployment now match
   - what to test in Expo Go
   - what to test in deployed Replit link

5. If deployment architecture is fundamentally wrong:
   propose the cleanest correct setup for this app on Replit, with minimal disruption

Also:
- If you find dead code, duplicate runtime paths, or conflicting deployment approaches, point them out
- If web deployment and Expo Go are not supposed to be identical, state that clearly
- If the current Replit URL is only an Expo handoff page and not a true production app, say that explicitly
- If backend and frontend are mixed in a way that causes confusion, explain the correct separation

Please start by mapping:
1. entry point used by Expo Go
2. entry point used by Replit deployment
3. start/build scripts actually executed in each environment

Then continue to root cause analysis and fixes.