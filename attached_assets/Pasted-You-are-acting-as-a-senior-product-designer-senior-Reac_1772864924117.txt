You are acting as a senior product designer, senior React Native engineer, and senior software architect continuing work on this Adventist spiritual formation mobile app.

Context:
Phase 1 audit and Priority 1 improvements have already been completed. These included:

• Extracted shared SectionHeader component
• Extracted shared AnimatedSection component
• Moved Christian Radio from Study tab to Connect tab
• Removed Great Controversy card from Home surface
• Deleted FULL_PROJECT_CODE.txt dead file
• Cleaned unused imports/styles

The 5-tab navigation model has been confirmed as correct:

Home
Read
Connect
Study
You

This architecture must remain intact.

Your new mission is **Phase 2A: Safe Structural Refactoring**.

The goal is to **improve maintainability and reduce risk without changing behavior**.

Do NOT redesign the app.
Do NOT add new features.
Do NOT change theology logic.
Do NOT change AI generation rules.
Do NOT touch authentication, TTS, or LiveKit systems.

The app is already functioning well. This phase is strictly about **making the codebase safer and easier to maintain**.

------------------------------------------------

CRITICAL RULES

1. Do NOT break existing behavior.
2. Do NOT change UI layout unless necessary for consistency.
3. Do NOT rewrite major systems.
4. Do NOT touch doctrinal guardrails.
5. Do NOT change attribution or disclaimer logic.
6. Do NOT change the AI engine or theological constraints.
7. Do NOT refactor large systems unless they can be safely decomposed.

Prefer **small, safe extractions of components** rather than rewriting screens.

------------------------------------------------

CURRENT ENGINEERING RISKS IDENTIFIED

Large files that need safe decomposition:

study.tsx → 4,330 lines (HIGH RISK — DO NOT TOUCH YET)
index.tsx (Home) → 1,837 lines
profile.tsx → 1,078 lines

Smaller components should be extracted from these files to reduce risk.

------------------------------------------------

PHASE 2A OBJECTIVES

Focus on **safe decomposition of large screens into smaller reusable components**.

Priority order:

1️⃣ PROFILE SCREEN CLEANUP

profile.tsx currently contains multiple responsibilities:

• Knowledge map
• Badges / achievements
• Growth progress
• Settings
• Saved content
• Family controls
• User information
• Possibly other UI sections

Goal:
Break this file into modular components while preserving behavior.

Examples of likely extractions:

components/profile/
  ProfileHeader.tsx
  KnowledgeMap.tsx
  BadgesGrid.tsx
  GrowthStats.tsx
  SavedContentSection.tsx
  SettingsSection.tsx
  FamilyControlsSection.tsx

Rules:
• No logic changes
• Only extract components
• Maintain existing props/state
• Preserve UI exactly

------------------------------------------------

2️⃣ HOME SCREEN STRUCTURE

index.tsx is currently ~1,837 lines.

The Home screen surfaces multiple sections:

• Greeting / header
• Sabbath experience
• Daily spiritual goals
• Continue reading
• Study suggestions
• Possibly other sections

Goal:
Extract **section-level components** so the main Home file becomes a simple layout.

Example structure:

components/home/
  HomeHeader.tsx
  SabbathExperienceCard.tsx
  SpiritualGoalsSection.tsx
  ContinueReadingCard.tsx
  SuggestedStudySection.tsx
  HomeQuickActions.tsx

Rules:
• Preserve layout
• Preserve animations
• Preserve logic
• Only separate presentation layers

------------------------------------------------

3️⃣ CREATE A SHARED SCREEN HEADER COMPONENT

The app currently repeats a common pattern across many screens:

Back Button
Screen Title
Optional Action Button (Aa, settings, etc)

Create a reusable component:

components/ScreenHeader.tsx

Props example:

title
showBackButton
rightAction
subtitle (optional)

This component should replace duplicated header patterns where safe.

Do NOT change layout or styling drastically.

------------------------------------------------

4️⃣ ROUTE CONSISTENCY CHECK

Audit the following routes:

kids-story/[id].tsx
kids/story/[id].tsx

Determine if these are duplicate routes.

If duplication exists:
• Propose the safest consolidation plan
• Do NOT remove routes without confirming no navigation breaks

Report findings before making changes.

------------------------------------------------

5️⃣ DO NOT TOUCH HIGH-RISK AREAS

The following systems must remain untouched in this phase:

study.tsx (4,330 lines)
AI generation engine
Doctrinal guardrails
Authentication
LiveKit video
Text-to-speech pipeline
Adventech Sabbath School data integration

These systems require tests before refactoring.

------------------------------------------------

DESIGN PRINCIPLES

The UI should remain:

• Reverent
• Calm
• Premium
• Structured
• Not cluttered
• Not flashy
• Clearly Adventist

Maintain the current dark theme and gold accent style.

------------------------------------------------

ENGINEERING PRINCIPLES

Prefer:

composition over duplication
small reusable components
clean folder organization
clear naming
isolated UI components
minimal logic movement

Avoid:

mass rewrites
changing data flow
large refactors

------------------------------------------------

DELIVERABLES

Your response should include:

1️⃣ Audit Summary

Briefly describe:

• profile.tsx structure
• index.tsx structure
• repeated header patterns
• route duplication risk

2️⃣ Refactor Plan

List:

• components to extract
• files affected
• risk level

3️⃣ Implementation

Perform safe extractions only.

4️⃣ Change Log

List every file changed and why.

5️⃣ Safety Report

Confirm:

• no features removed
• no navigation broken
• no AI logic altered
• no doctrinal systems touched

------------------------------------------------

TARGET RESULT

After this phase the codebase should have:

• smaller screen files
• clearer component structure
• reusable headers
• easier future maintenance
• identical user-facing behavior

You are acting like a senior engineer brought in to stabilize and polish a promising product.

Respect what already works.
Improve structure without introducing risk.