We are NOT rebuilding the app. We are finishing and stabilizing the existing build.

Read this entire instruction first, then make one clean implementation pass.

GOAL
Complete the next production-critical phase of Grace Through Faith by focusing on:
1. auth + account stability
2. roles and permissions
3. content pipeline end to end
4. app language applied globally
5. Sabbath School current + past quarter structure
6. keep UX simple and aligned with the current refined architecture

IMPORTANT PRODUCT RULES
- Do not redesign the app.
- Do not create new major feature systems unless explicitly required below.
- Reuse existing screens, patterns, routes, components, and styling wherever possible.
- Keep the current tab architecture intact.
- Keep Daily Formation rings on the Home screen only.
- Do not re-add “How This App Works” or “Replay Tutorials” to the You tab.
- Do not reintroduce duplicate language settings. “App Language” is the single visible language setting.
- Do not create role complexity beyond what is needed for this phase.
- Prefer a working end-to-end implementation over placeholder UI.
- Make all changes once, cleanly, without duplicate passes.

PHASE OBJECTIVES

T001 — AUTH STABILITY
Audit and fix sign in / create account so authentication works reliably end to end.

Requirements:
- Sign up works
- Sign in works
- Session persists correctly
- Passwords save correctly
- Add forgot/reset password flow if backend/auth provider supports it cleanly
- Prevent broken or partial account creation states
- Ensure guest mode still works
- Ensure the You tab correctly reflects guest vs signed-in user
- Ensure account creation does not feel blocked or confusing

Deliver:
- Working auth flow
- Any necessary validation improvements
- Clean error states
- No fake buttons or dead-end forms

T002 — ROLES AND PERMISSIONS
Implement a minimal role model so we can distinguish who can access content pipeline tools.

Required roles for now:
- guest
- member
- student
- church_leader
- admin

Rules:
- guest: browse public app areas, no protected tools
- member: standard signed-in user
- student: same as member unless otherwise needed later
- church_leader: can access content pipeline area if specified below
- admin: full access to content pipeline controls

Requirements:
- Role must be stored on the user profile/account model
- Add the minimum necessary backend + frontend logic to support role checks
- Do not expose unnecessary admin UI to normal users
- Do not build a giant permissions dashboard
- If no proper onboarding role picker exists yet, create the simplest clean role assignment path that works for testing
- Prefer secure role gating over cosmetic hiding only

Deliver:
- Working role-aware auth state
- Protected access for content pipeline screens/tools
- Clear fallback behavior for unauthorized users

T003 — CONTENT PIPELINE END TO END
This is a critical task. Make the content pipeline actually testable from creation to publication to in-app display.

Requirements:
- Audit existing Content Pipeline implementation first
- Make sure pipeline content can move through a real usable flow such as:
  draft -> review -> approved -> published
- Ensure only authorized roles can access pipeline management
- Ensure published content appears in the app where relevant
- Ensure unpublished content does not leak to normal users
- Ensure status and source of content are clear internally
- Ensure content can be tested for doctrinal alignment and accuracy
- If there is already an AI-assisted generation step, keep it transparent and properly labeled
- Do not overbuild enterprise editorial software; keep it lean and testable

Minimum usable content types to support:
- devotional/study content
- Sabbath School content if already connected or planned in pipeline
- any existing generated lesson/study content already part of the architecture

Deliver:
- End-to-end working content pipeline flow
- Role-gated access
- Published content visible in app
- Clear internal statuses
- No fake management screens

T004 — GLOBAL APP LANGUAGE
Implement real app-wide language behavior using the existing App Language setting.

Requirements:
- App Language must control visible UI language across the app
- Content should follow App Language by default
- Keep only one visible language setting: App Language
- Do not re-add visible “Content Language” setting
- Use existing language architecture where possible
- Audit what is already translated and what still falls back to English
- Ensure safe fallback behavior when a translation is missing
- At minimum, the language system should function cleanly for the currently supported languages already in settings
- Avoid hardcoded English strings where possible in key screens

Priority areas:
- Home
- Read
- Connect
- Study
- You
- auth screens
- settings
- Sabbath School
- content pipeline output display

Deliver:
- Working global language application
- Clean fallback behavior
- No duplicate language settings
- Report what is fully translated vs partially translated

T005 — CONTENT TRANSLATION FLOW
We need the app capable of serving content in the user’s selected language.

Requirements:
- Reuse existing content language resolution logic behind the scenes
- Make generated/published content respect App Language when available
- If translated content is unavailable, fall back cleanly to English
- Ensure this is true for:
  - devotionals
  - study content
  - Sabbath School content where possible
  - content pipeline published items

Important:
- Do not invent fake translation coverage
- If machine translation or AI translation is used internally, label it appropriately in internal/admin workflows, not with noisy UI for normal users unless necessary
- Accuracy and alignment matter more than breadth

Deliver:
- Language-aware content retrieval/display
- Clean fallback behavior
- Internal implementation that is testable

T006 — SABBATH SCHOOL: CURRENT + PAST QUARTERS
Extend Sabbath School so it supports current quarter plus past quarters.

Requirements:
- Keep current quarter easy to access
- Add past quarter archive access
- Structure it so it can scale later
- Respect App Language where content exists
- Do not clutter the Study hub screen
- Reuse the current Sabbath School entry point and flow where possible

Minimum UX:
- Sabbath School opens into a clean screen where user can access:
  - current quarter
  - past quarters/archive
- Make the archive usable, not just a stub

Deliver:
- Current + archive quarter structure
- Language-aware where possible
- Clean navigation

T007 — KEEP THE UX CLEAN
As you implement, preserve the refined product direction.

Do NOT:
- bring back long cluttered walls of cards where hub patterns are now used
- re-add duplicate settings
- re-add tutorial clutter
- move Daily Formation rings off Home
- make Study or Connect noisy again

Do:
- preserve the category hub approach
- preserve the cleaner You tab
- keep language UX simple
- keep wording accessible, not overly technical
- keep church-aligned framing without sounding institutional everywhere

IMPLEMENTATION ORDER
Do this in order:

1. Audit existing auth, role, language, content pipeline, Sabbath School architecture
2. Fix auth stability first
3. Add minimal role model + route protection
4. Make content pipeline work end to end
5. Apply App Language globally and verify fallbacks
6. Hook content display to language behavior
7. Add Sabbath School archive/current structure
8. Final QA pass for regressions

FILES / AREAS TO CHECK
Audit and modify only what is needed, likely including:
- auth screens / auth service
- user profile model / role model
- protected routes or gated screens
- content pipeline screens and backend flow
- settings / language state / i18n
- Sabbath School screens and data flow
- study content rendering
- any generated content services or publish logic

DELIVERABLE FORMAT
When finished, provide a concise report with exactly these sections:

1. Audit Findings
2. Files Changed
3. Auth Fixes
4. Roles & Permissions
5. Content Pipeline Flow
6. Language Behavior
7. Sabbath School Archive
8. Remaining Gaps

QUALITY BAR
- No placeholder-only implementation
- No duplicate systems
- No broken navigation
- No dead buttons
- No fake role security
- No unnecessary rebuilds
- Preserve the refined UI direction already established